# Shared Rules: facts（数值引用文件）

适用范围：所有生成内容引用数据指标的 skill（推荐/报告/评价/知识库）。

## 规则
1. 回答/报告/评语中出现的每一个数值（成绩、均分、学分、容量、人数、考核方式）**必须来自工具返回或数据源**，不得凭记忆或推理编造。
2. 引用数据时标注来源（来源页码 / 课程事实字段 / 成绩单条目）。
3. 数据不足时明确说明知识边界，不补全、不猜测。
