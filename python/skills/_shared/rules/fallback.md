# Shared Rules: fallback（失败兜底链）

适用范围：所有涉及外部调用（LLM/工具/MCP/渲染/存储）的 skill。

## 规则
1. 工具失败 → 按该 skill 的降级链执行（如 LLM 填表→确定性模板、PDF→HTML、MCP→直连），**绝不空返回**。
2. 部分失败（批量场景）→ 失败项隔离，成功项照常交付，失败明细保留供重试。
3. 结构性错误（熔断/无数据/权限）→ 明确告知用户原因与重试方式，不静默吞掉。
4. 重试有上限（默认 1 次），失败后走降级而非无限重试。
