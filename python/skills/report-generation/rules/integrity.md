# Rules: integrity（数据完整性）

1. **LLM 只填表不计算**：解析/合并/统计/校验全部确定性代码，LLM 不参与数值计算。
2. **防信息丢失**：按学号主键合并；跨文件差集告警；缺科学生**显式报错**（不静默、不渲染）。
3. **只留等级**：解析只取等级列，分数/原始/折算/备注列丢弃。
4. **模板锚点**：填表输出须通过结构校验 + 数值回填校验（逐槽比对），违规回灌重试一次。
