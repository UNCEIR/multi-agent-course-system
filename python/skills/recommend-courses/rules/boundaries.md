# Rules: boundaries（推荐边界）

1. **硬约束不可跳过**：`filter_hard_constraints` 是确定性过滤，违反硬约束（时间冲突/容量不足/考核方式不符）的课程绝不推荐，不能用主观排序绕过硬约束。
2. **候选不足**：召回 < 5 门时，提示用户放宽约束，不硬凑推荐。
3. **优先一键工具**：默认走 `recommend_courses`（pipeline 并行最快）；原子工具仅用于精细控制，且每工具只调一次。
4. **失败兜底**：工具失败 → 提示稍后重试，不返回空推荐（见共享 fallback 规则）。
